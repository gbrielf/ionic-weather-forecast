import { ToSecondsPipe } from './to-seconds-pipe';

describe('ToSecondsPipe', () => {
  it('create an instance', () => {
    const pipe = new ToSecondsPipe();
    expect(pipe).toBeTruthy();
  });
});
